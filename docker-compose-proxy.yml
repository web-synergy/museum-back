version: '3'

services:
  proxy:
    build: nginx/.
    ports:
      - '80:80'
    depends_on:
      - app

  app:
    build: .
    depends_on:
      - redis
    restart: on-failure

  redis:
    image: 'redis/redis-stack'
    ports:
      - '8001:8001'
    restart: on-failure